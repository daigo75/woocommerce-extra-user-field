<?php
namespace PLUGIN_NSPACE;
if(!defined('ABSPATH')) exit; // Exit if accessed directly

/**
 * Contains a list of definitions and constants used throughout the plugin.
 */
class Definitions {
	// @var string The menu slug for plugin's settings page.
	const MENU_SLUG = 'PLUGIN_MENU_SLUG';
	// @var string The plugin slug
	const PLUGIN_SLUG = 'PLUGIN_SLUG_VALUE';
	// @var string The plugin text domain
	const TEXT_DOMAIN = 'PLUGIN_TEXT_DOMAIN';
}
